# AI Agent Instructions

This fixture uses progressive disclosure documentation and the recipe profile.

## How to Load

1. Read [docs/ai/L0_repo_card.md](docs/ai/L0_repo_card.md).
2. Resolve the pinned base from `Extends`.
3. Read the base repo's `RECIPE.md`.
4. Load all 8 local L1 slots and apply `Locked` / `Overlay`.
5. Read [RECIPE_DELTA.md](RECIPE_DELTA.md) for the child-specific summary.

## Git Conventions

- Use conventional commits.
- Keep branch names in `type/short-description` format.

## Doc Commands

- `generate docs`
- `update docs`
- `test docs`
- `fix docs`
